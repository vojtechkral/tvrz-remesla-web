import React from 'react';

import AppProvider from './AppProvider';
import {ErrorAlert} from './containers';

export default () => (
    <AppProvider>
        <div>Form app</div>
    </AppProvider>
)
